#include "../include/anunciador.h"
/*

Anunciador::Anunciador(Ventana* ventana) : ventana(ventana){}

Anunciador::~Anunciador(){}

void Anunciador::settearInformacion(Player* jugador, std::map<Enemigo*> enemigos){
    this->jugador = jugador;
    this->enemigos = enemigos;
}

void Anunciador::settearGanadores(std::vector<int> ganadores){
    Fonts fuentes;
    int x = 200;
    int y = 200;
    fuentes.addFont("ganadores", "../../resources/fuentes/Nougat.ttf", 35);
    SDL_Color blanco = {255, 255, 255, 255};
    Label ganador(200, 200, "GANADORES", fuentes.getFont(ganadores), , ventana.getRender());
    for (int i = 0;i < ganadores.size(); i++){
        if (jugador.getId() == ganadores[i]){
            Label label(x,y)
        }
    }
}
*/
